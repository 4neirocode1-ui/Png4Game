# Зависимости конвейера нарезки иконок Png4Game (Python 3.12).
# Установка в свежее окружение:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
# Версии закреплены под то, на чём проект реально работает (2026-06-11).

opencv-python==4.13.0.92   # детект иконок, обработка изображений (cv2)
numpy==2.4.4               # массивы пикселей (зависимость opencv, используем напрямую)
Pillow==12.2.0             # запись WebP с прозрачностью (cv2 в части сборок теряет альфу)
